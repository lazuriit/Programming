Команды
```
mkdir build - создание директории для храннения Make файлов
cd build - переход в директорию build
cmake -DLIB_SELECT="SHARED" .. сборка Makefile с динамической библиотекой(cmake .. со статической, если изначально вводить без опций)
make - сборка проекта
```
